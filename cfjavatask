STEP 1:- #for choose java version
          sudo alternatives --config java

STEP 2:- #Add below code to pom.xml file for skip test cases

        <plugin>  
            <groupId>org.apache.maven.plugins</groupId>  
            <artifactId>maven-surefire-plugin</artifactId>  
            <version>2.22.2</version>  
            <configuration>  
            <skipTests>true</skipTests>  
            </configuration>  
         </plugin> 

STEP 3:- run "mvn clean install" cmd

STEP 4:- run "aws configure" cmd

STEP 5:- "push the image to ECR"

STEP 6:- Make changes in "docker-compose.yml" & "src/main/resource/application.yml" file with ecr image,database details,by commenting postgresql container details

STEP 7:- Finally run "docker-compose up &" cmd
